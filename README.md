# 가가오톡

우리 학교 친구들만 들어올 수 있는 톡.

- 초대 코드 + 관리자 승인으로 가입을 통제한다
- 데이터는 Supabase 의 Row Level Security 가 지킨다. 승인되지 않은 계정은 메시지를 한 줄도 읽을 수 없다
- 이 저장소에 있는 키는 `publishable key` 로, 원래 클라이언트에 공개되는 값이다

같은 서버에 iOS 앱도 붙는다.
